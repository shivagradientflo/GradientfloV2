export interface NavLinkItem {
  /**
   * Text label shown in the navigation.
   */
  label: string;

  /**
   * Optional href for future navigation wiring.
   * Kept optional to preserve current visual-only behavior.
   */
  href?: string;
}


